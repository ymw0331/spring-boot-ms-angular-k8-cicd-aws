server:
  port: 9094

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: ORDER-SERVICE
  data:
    mongodb:
      uri: mongodb+srv://wayne:wayne@cluster0.clygx9v.mongodb.net/orderdb
      host: wayne:wayne@cluster0.clygx9v.mongodb.net
      database: orderdb
      repositories:
        enabled: true
